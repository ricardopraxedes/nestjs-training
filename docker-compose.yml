version: "3.7"

services:
    mongodb:
        image : mongo
        container_name: mongodb
        restart: always
        environment:
            - MONGODB_DATABASE="test"
        ports:
            - 27017:27017
        